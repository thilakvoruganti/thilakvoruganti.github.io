# Docs

Supporting material for the portfolio:

- `assets/` holds zipped exports provided during the Connect & Experience section rebuilds. They are referenced for parity only and excluded from the production bundle.
- `notes/` contains raw import commands and scratch notes used while dissecting the original Figma sections.

These files stay in Git history for provenance but outside the main `src/` tree to keep the production bundle focused.
